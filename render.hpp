#ifndef RENDER_HPP
#define RENDER_HPP

#include <string>

// Function to render typography
void renderTypography(const std::string& text, const std::string& fontPath, int fontSize, int width, int height, const std::string& filename);

#endif // RENDER_HPP
